# Production PostgreSQL connection string (Neon free tier).
# Create a database at https://neon.tech, then paste its connection string here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/speakeasy?sslmode=require"

# Secret used to sign session cookies. Generate one with: openssl rand -base64 32
AUTH_SECRET=""
